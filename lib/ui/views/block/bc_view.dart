import 'package:flutter/material.dart';

class BCView extends StatelessWidget {
  const BCView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('block'),
      ),
    );
  }
}
